<Window x:Class="gironWin.ApprovalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="送信承認"
        Width="700"
        Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock x:Name="DirectionTextBlock"
                       FontSize="15"
                       FontWeight="Bold"
                       Foreground="#1a6e8a"/>
            <TextBlock x:Name="TimestampTextBlock"
                       FontSize="11"
                       Foreground="Gray"
                       Margin="0,2,0,0"/>
        </StackPanel>

        <!-- 指示 -->
        <TextBlock Grid.Row="1"
                   Text="送信内容を確認・編集してから承認または却下してください。"
                   Foreground="Gray"
                   FontSize="12"
                   Margin="0,0,0,8"/>

        <!-- 編集エリア -->
        <TextBox Grid.Row="2"
                 x:Name="EditTextBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas, Meiryo UI, sans-serif"
                 FontSize="13"
                 Padding="8"
                 BorderBrush="#c0c0c0"
                 BorderThickness="1"/>

        <!-- ボタン行 -->
        <DockPanel Grid.Row="3" Margin="0,12,0,0" LastChildFill="False">
            <TextBlock x:Name="CharCountTextBlock"
                       VerticalAlignment="Center"
                       Foreground="Gray"
                       FontSize="11"
                       DockPanel.Dock="Left"/>
            <Button x:Name="RejectButton"
                    Content="却下"
                    Width="90"
                    Margin="0,0,8,0"
                    DockPanel.Dock="Right"
                    Click="RejectButton_Click"/>
            <Button x:Name="ApproveButton"
                    Content="承認して送信"
                    Width="120"
                    DockPanel.Dock="Right"
                    Margin="0,0,8,0"
                    Background="#1a6e8a"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="ApproveButton_Click"/>
        </DockPanel>
    </Grid>
</Window>
